for _ in range(int(input())):
    n, x, y = [*map(int, input().split())]
    s = input()
    b = []
    b[0][0] = 1
    for v in s:
        if v == '4':
            # For every point
        
        else:
            # For every point 
